generator client {
  provider      = "prisma-client-js"
  // "native" para desarrollo local; el resto para el runtime de Vercel.
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider  = "postgresql"
  // Conexión "pooled" de Supabase (puerto 6543, pgbouncer) — la usa la app en runtime.
  url       = env("DATABASE_URL")
  // Conexión directa de Supabase (puerto 5432) — la usan `prisma migrate` / `prisma db push`.
  directUrl = env("DIRECT_URL")
}

model RSVP {
  id          Int      @id @default(autoincrement())
  name        String
  email       String
  phone       String? // Teléfono del invitado
  attending   Boolean
  guestsCount Int
  dietary     String?
  message     String?
  cedulas     String // Almacenado como JSON string o valores separados por comas
  createdAt   DateTime @default(now())
}

model GuestMessage {
  id        Int      @id @default(autoincrement())
  name      String
  message   String
  approved  Boolean  @default(true)
  createdAt DateTime @default(now())
}

model AllowedGuest {
  id        Int       @id @default(autoincrement())
  phone     String    @unique
  pin       String
  maxGuests Int       @default(2) // Cantidad de invitados permitidos para este número
  used      Boolean   @default(false)
  usedAt    DateTime?
  createdAt DateTime  @default(now())
}
